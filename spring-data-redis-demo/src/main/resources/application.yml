spring:
  redis:
    database: 0
    host: localhost
    port: 6379
    jedis:
      pool:
        max-active: 8 #最大连接数
        max-wait: 1ms #最大阻塞等待时间
        max-idle: 8 #最大空闲连接
        min-idle: 0
  application:
    name: spring-data-redis-demo